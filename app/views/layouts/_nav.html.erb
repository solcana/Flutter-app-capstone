<nav class="navbar navbar-expand-lg navbar-light">
  <a class="navbar-brand" href="travel_logs">Flutter 🦋</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="<%= travel_logs_path%>">Feed <span class="sr-only"></span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= my_logs_path%>">My Travels</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= destinations_path%>">Destinations</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= new_travel_log_path%> ">Add Travel</a>
      </li>
    </ul>
  </div>
  <ul class="nav navbar-nav ml-auto">
  <% if user_signed_in? %>
    <li class="nav-item"><a class="nav-link" href="#"><%= current_user.username %></a></li>
    <li class="nav-item"><a class="nav-link" href="<%= destroy_user_session_path %>" data-turbo-method="delete">Logout</a></li>
     <li class="nav-item">
  <%= link_to "Delete Profile",
              registration_path(current_user),
              class: "nav-link",
              data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete your profile?" } %>
</li>
  <% else %>
    <li class="nav-item"><a class="nav-link" href="<%= new_user_registration_path %>">Sign Up</a></li>
    <li class="nav-item"><a class="nav-link" href="<%= new_user_session_path %>">Login</a></li>
  <% end %>
</ul>
</nav>